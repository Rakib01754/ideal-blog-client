import { Container } from '@mui/material';
import React from 'react';

const Navbar = () => {
    return (
        <Container>
            Ideal Blogs
        </Container>
    );
};

export default Navbar;